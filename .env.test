# Test environment variables (committed, no real secrets)
# Override with .env.local (gitignored) for real API keys

OPENAI_API_KEY=sk-test-fake-key-for-testing
ANTHROPIC_API_KEY=test-fake-key-for-testing
GITHUB_TOKEN=ghp-test-fake-token

# Amplifier config
AMPLIFIER_HOME=/home/testuser/.amplifier
